services:
  - type: web
    name: django-chat-app
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: chatapp.settings
      - key: SECRET_KEY
        generateValue: true
